#include <stdio.h>
int main()
{
    char password[12];
    int di
    while (1){
    printf("masukan:");
    fgets(password, sizeof(password), stdin);
    if (isdigit(password[i]))
    {

        /* code */
    }
    
    if (strlen(password) < 8 && strlen(password) > 12)
    {

    }
    
    }

}